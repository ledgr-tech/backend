# preencher quando o Postgres do Railway for criado
DATABASE_URL=postgresql+psycopg://usuario:senha@host:5432/ledgr

# segredo compartilhado com o frontend (NextAuth) — ver ADR-003, nunca commitar o valor real
NEXTAUTH_SECRET=
